import { useState } from 'react';

const emptyGuest = { firstName: '', lastName: '', phone: '', email: '', gender: 'unspecified', group: '', category: 'other', side: 'shared', plusOneAllowed: false, plusOneName: '', tableNumber: '', notes: '' };
const inputClass = 'mt-2 w-full rounded-xl border border-stone-200 bg-white px-4 py-3 outline-none transition focus:border-brand-400 focus:ring-2 focus:ring-brand-100';

function GuestForm({ guest, submitting, error, onSubmit, onCancel, hideHeader = false }) {
  const [form, setForm] = useState(guest ? { firstName: guest.firstName, lastName: guest.lastName || '', phone: guest.phone || '', email: guest.email || '', gender: guest.gender || 'unspecified', group: guest.group || '', category: guest.category, side: guest.side === 'partner1' ? 'bride' : guest.side === 'partner2' ? 'groom' : guest.side, plusOneAllowed: guest.plusOneAllowed, plusOneName: guest.plusOneName || '', tableNumber: guest.tableNumber || '', notes: guest.notes || '' } : emptyGuest);
  const update = (event) => setForm({ ...form, [event.target.name]: event.target.value });
  function togglePlusOne(event) { const allowed = event.target.checked; setForm({ ...form, plusOneAllowed: allowed, plusOneName: allowed ? form.plusOneName : '' }); }
  async function handleSubmit(event) { event.preventDefault(); await onSubmit(form); }
  return <form onSubmit={handleSubmit} className="space-y-5">{hideHeader ? null : <div><h2 className="font-display text-2xl font-semibold text-stone-900">{guest ? 'Edit Guest' : 'Add Guest'}</h2><p className="mt-1 text-sm text-stone-500">Add the people you want beside you on your wedding day.</p></div>}{error && <div role="alert" className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-stone-700">First Name<input required maxLength="80" name="firstName" value={form.firstName} onChange={update} className={inputClass} /></label><label className="text-sm font-medium text-stone-700">Last Name <span className="font-normal text-stone-400">(optional)</span><input maxLength="80" name="lastName" value={form.lastName} onChange={update} className={inputClass} /></label><label className="text-sm font-medium text-stone-700">Phone <span className="font-normal text-stone-400">(optional)</span><input maxLength="40" type="tel" name="phone" value={form.phone} onChange={update} className={inputClass} /></label><label className="text-sm font-medium text-stone-700">Email <span className="font-normal text-stone-400">(optional)</span><input maxLength="160" type="email" name="email" value={form.email} onChange={update} className={inputClass} /></label><label className="text-sm font-medium text-stone-700">Category<select name="category" value={form.category} onChange={update} className={inputClass}><option value="family">Family</option><option value="friend">Friend</option><option value="relative">Relative</option><option value="colleague">Colleague</option><option value="vip">VIP</option><option value="other">Other</option></select></label><label className="text-sm font-medium text-stone-700">Guest Side<select name="side" value={form.side} onChange={update} className={inputClass}><option value="bride">Bride</option><option value="groom">Groom</option><option value="shared">Shared</option></select></label><label className="text-sm font-medium text-stone-700">Gender<select name="gender" value={form.gender} onChange={update} className={inputClass}><option value="unspecified">Unspecified</option><option value="female">Female</option><option value="male">Male</option><option value="other">Other</option></select></label><label className="text-sm font-medium text-stone-700">Group<input name="group" value={form.group} onChange={update} className={inputClass} /></label><label className="text-sm font-medium text-stone-700">Table<input name="tableNumber" value={form.tableNumber} onChange={update} className={inputClass} /></label></div><div className="rounded-xl border border-stone-100 bg-stone-50 p-4"><label className="flex cursor-pointer items-center justify-between gap-4"><span><span className="block text-sm font-semibold text-stone-700">Allow Plus One</span><span className="mt-1 block text-xs text-stone-500">This guest may bring one additional person.</span></span><input type="checkbox" checked={form.plusOneAllowed} onChange={togglePlusOne} className="h-5 w-5 accent-brand-600" /></label>{form.plusOneAllowed && <label className="mt-4 block text-sm font-medium text-stone-700">Plus One Name <span className="font-normal text-stone-400">(optional)</span><input maxLength="160" name="plusOneName" value={form.plusOneName} onChange={update} className={inputClass} /></label>}</div><label className="block text-sm font-medium text-stone-700">Notes <span className="font-normal text-stone-400">(optional)</span><textarea rows="3" maxLength="500" name="notes" value={form.notes} onChange={update} className={`${inputClass} resize-none`} /></label><div className="flex flex-col-reverse gap-3 border-t pt-5 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} className="rounded-full border px-6 py-3 text-sm font-semibold text-stone-600 hover:bg-stone-50">Cancel</button><button disabled={submitting} className="rounded-full bg-brand-600 px-7 py-3 text-sm font-semibold text-white hover:bg-brand-700 disabled:opacity-60">{submitting ? 'Saving…' : guest ? 'Save Changes' : 'Add Guest'}</button></div></form>;
}
export default GuestForm;
